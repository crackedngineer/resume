name: Build Your CV

on:
  push:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: write
  packages: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: crackedngineer/codeurcv-action@v0.2.1
        with:
          out-dir: "output"
          file-name: "config.yml"
